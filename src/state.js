// for know how was working dispatch before redux
// dispatch(action) {
//   store._state.myProfile = mainAction(store._state.myProfile, action);
//   store._state.dialogs = dialogsAction(store._state.dialogs, action);
//   rerenderEntrieTree(store);
// }
